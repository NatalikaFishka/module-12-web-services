const URL = 'https://jsonplaceholder.typicode.com';

const HEADER = {
  KEY: "Content-type",
  VALUE: "application/json; charset=utf-8"
}

const RESOURCES = {
  USERS: '/users'
}

module.exports = { URL, RESOURCES, HEADER };